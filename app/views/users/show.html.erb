
<%= render 'layouts/mainmenu' %>

<div class="show">

  <div class="container">
    <header>
      <div class="bio">
        <img src=<%= @user.cover_picture%> alt="background" class="bg">
        <div class="desc">
          <h3>@<%= link_to @user.name, edit_user_path, class: "a_edit_user" %></h3>
          <p><%= @user.bio%></p>
        </div>
      </div>

      <div class="avatarcontainer">
        <img src=<%= @user.img_url%> alt="avatar" class="avatar">

        <div class="hover">
          <div class="icon-twitter"></div>
        </div>
      </div>


    </header>

    <div class="content">
      <div class="data">
        <ul class="datauser">
          <li>
            <%= @user.tweets.count %>
            <span>Tweets</span>
          </li>
          <li>
            1,119
            <span>Followers</span>
          </li>
          <li>
            530
            <span>Following</span>
          </li>
        </ul>
      </div>
    </div>

    <%= render 'tweets/form', 'tweet': @new_tweet %>
  </div>





  <!--<table >
    <tr><td><img src=<%= @user.img_url %> alt="Smileyface"></td></tr>
    <tr><td id="name"><%= link_to @user.name, edit_user_path %></td></tr>
    <tr><td id="bio"><%= @user.bio %></td></tr>

  </table>-->



  <div class="tweets">
<% @tweets.each do |tweet| %>
    <div class="tweet">
      <img src="<%= tweet.user.img_url %>" alt="" >
      <p id="tweet-author"> <%= link_to @user.name, edit_user_path %> </p>
      <p id="date-tweet"> <%= tweet.created_at.strftime("%d/%m/%Y") %></p>
      <p> <%= tweet.content %></p>


    </div>
<% end %>
  </div>




</div>

</div>